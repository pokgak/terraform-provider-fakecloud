---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "fakecloud_tictactoe_move Resource - fakecloud"
subcategory: ""
description: |-
  A mark on a board. Creating it plays the move; destroying it takes the move back. On a duel board the server also rejects the apply if it is not your turn.
---

# fakecloud_tictactoe_move (Resource)

A mark on a board. Creating it plays the move; destroying it takes the move back. On a duel board the server also rejects the apply if it is not your turn.

## Example Usage

```terraform
resource "fakecloud_tictactoe_move" "center" {
  board_id = fakecloud_tictactoe_board.lesson.id
  player   = "X"
  position = 4 # cells are 0-8, row by row
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `board_id` (Number) The board this move belongs to.
- `player` (String) "X" or "O". On a duel board, X always starts.
- `position` (Number) Cell to claim, 0-8, row by row (4 is the center).

### Read-Only

- `id` (Number) Server-assigned move id.
